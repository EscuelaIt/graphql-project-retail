# import Product from "./entities/product.graphql"
# import Client from "./entities/client.graphql"
# import Cart from "./entities/cart.graphql"

type Query{
  allProducts: [Product!]
  allClients: [Client!]
  getCart(id: ID!): Cart
}

type Mutation{
  createClient(name: String!, age: Int!): Client
}